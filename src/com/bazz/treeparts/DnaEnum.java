/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.bazz.treeparts;

/**
 *
 * @author bazyli
 */
public enum DnaEnum {
	ANGLE_BRANCH(), ANGLE_DELTA_BRANCH(), ANGLE_MINIBRANCH(), ANGLE_ROOT_START(), ANGLE_ROOT_DELTA(), START_NUMBER_OF_ROOTS(), TRUNK_SIZE(), BRANCH_SIZE(), MINIBRANCH_SIZE(), MAIN_ROOT_SIZE(), ROOT_SIZE(), TRUNK_RATIO(), BRANCH_RATIO(), MINI_BRANCH_RATIO(), MAIN_ROOT_RATIO(), ROOT_RATIO(), DELTA_MAIN_ROOT(), DELTA_ROOT(), DELTA_TRUNK(), DELTA_BRANCH(), DELTA_MINIBRANCH(), DISTANCE_BETWEEN_BRANCHES(), DISTANCE_BETWEEN_MINIBRANCHES(), TRUNK_ANGLE_DELTA(), ROOT_RECURSION_LEVEL(), TRUNK_RECURSION_LEVEL();
}
